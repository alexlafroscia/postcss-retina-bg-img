'use strict';

var postcss = require('postcss');

module.exports = postcss.plugin('postcss-retina-bg-img', function (opts) {
    opts = opts || {};

    return function (css) {
        // Plugin logic goes here
    };
});
